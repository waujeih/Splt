# Splt — Split expenses. Stay friends.

A mobile-first expense-splitting web app for friends, roommates, and travel groups.

## Run locally

```bash
npm install
npm run dev
# open http://localhost:3000